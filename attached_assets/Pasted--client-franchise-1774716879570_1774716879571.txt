ДОПОЛНИ СУЩЕСТВУЮЩИЙ ПРОЕКТ.

СТРОГО:

* Изменения применяются КО ВСЕМ ролям (client / franchise / production)
* НЕ менять бизнес-логику
* НЕ менять структуру экранов
* НЕ ломать текущую верстку

==================================================
=== ВСЕ РОЛИ (GLOBAL) ===
=========================

1. ЛОГОТИП AVISHU:

ЗАМЕНА:

* Заменить текущий логотип/текст "AVISHU" на оригинальный логотип, который будет предоставлен

ТРЕБОВАНИЯ:

* Использовать приложенное изображение
* Логотип должен быть:

  * БЕЛОГО цвета (при необходимости перекрасить)
  * Четким (без blur и эффектов)
  * Сохранять пропорции (не растягивать)

РАЗМЕЩЕНИЕ:

* Во всех местах, где используется логотип:

  * верхняя панель (header)
  * экран клиента
  * экран франчайзи
  * экран цеха

ПОЗИЦИОНИРОВАНИЕ:

* Логотип должен быть строго по центру верхней панели

* Выровнен по горизонтали (center)

* НЕ смещать ни влево, ни вправо

* Другие элементы:

  * размещать по краям (слева/справа)
  * НЕ должны мешать центровке логотипа

ПОВЕДЕНИЕ:

* Логотип НЕ является кнопкой
* Используется только как визуальный элемент

==================================================
2. ПЕРЕКЛЮЧЕНИЕ ЯЗЫКА:
======================

ЗАМЕНА:

* Убрать текущую реализацию выбора языка (если есть)
* Сделать одну кнопку с выпадающим списком

КНОПКА:

* Иконка "планета" (🌐)

РАЗМЕЩЕНИЕ:

* В верхней панели (header)
* Справа (рядом с иконкой профиля)

ПОВЕДЕНИЕ:

* При нажатии:
  → открывается dropdown (выпадающий список)

СПИСОК ЯЗЫКОВ:

* Казахский
* Русский
* Английский

ТРЕБОВАНИЯ:

* Компактный список
* Без отдельного экрана
* Без перегрузки интерфейса
* Только один элемент управления языком (без дублирования)

==================================================
=== ВАЖНО ===
=============

* НЕ добавлять анимации
* НЕ применять фильтры или blur к логотипу
* НЕ создавать отдельные страницы
* НЕ ломать текущий header

==================================================
=== ПОСЛЕ ИЗМЕНЕНИЙ ===
=======================

* Указать, где обновлён логотип (файлы/экраны)
* Показать, как подключено изображение (путь к файлу)
* Указать, где добавлена кнопка языка
* Показать реализацию dropdown
