ДОПОЛНИ СУЩЕСТВУЮЩИЙ ПРОЕКТ.

СТРОГОЕ ПРАВИЛО:

* Добавлять изменения ТОЛЬКО в указанные роли и разделы
* НЕ менять другие экраны
* НЕ добавлять этот функционал в другие части приложения
* НЕ дублировать элементы в других ролях

==================================================
=== КЛИЕНТ (ТОЛЬКО В РОЛИ CLIENT) ===
=====================================

1. НАВИГАЦИЯ (ТОЛЬКО экраны клиента):

* Логотип "AVISHU" → сделать НЕ кликабельным
* Кнопку "назад" реализовать через "три полоски" слева сверху
* ВНИМАНИЕ:

  * В главной ленте клиента (главный экран) "три полоски" НЕ показывать
  * Только во вложенных экранах (карточка товара, коллекции, профиль и т.д.)

2. ФОТОГРАФИИ (ТОЛЬКО клиентская часть):

* Убрать blur/эффекты:

  * в каталоге
  * в карточке товара
  * в заказах
* Не трогать изображения в других ролях

3. МОИ ЗАКАЗЫ (ТОЛЬКО экран клиента "Мои заказы"):

* Цветная иконка статуса:

  * должна соответствовать статусу из базы
  * брать статус, который установлен ЦЕХОМ
  * обновляться в реальном времени
* Не добавлять эту логику в другие списки заказов

4. КАРТОЧКА ТОВАРА (ТОЛЬКО клиент):

* Добавить галерею изображений (горизонтальный скролл)
* Не добавлять галерею в других ролях

==================================================
=== ФРАНЧАЙЗИ (ТОЛЬКО ROLE = FRANCHISE) ===
===========================================

1. МЕНЮ (ТОЛЬКО интерфейс франчайзи):
   Добавить разделы:

* Дашборд
* Товары
* План

НЕ добавлять это меню:

* в клиент
* в цех

2. ДАШБОРД (ТОЛЬКО экран Dashboard франчайзи):
   Добавить:

* График количества заказов:

  * фильтр: сегодня / неделя / месяц / год
  * по умолчанию: сегодня

* Блок суммы заказов:

  * общая сумма
  * краткое описание

НЕ добавлять графики:

* в клиент
* в цех

3. ТОВАРЫ (ТОЛЬКО экран "Товары" франчайзи):
   Добавить:

Кнопка:

* "Добавить товар"

Форма:

* Фото (множественные, вертикальные, до 200 МБ)
* Название (до 500 символов)
* Описание (до 3000 символов)
* Цена (KZT)
* Количество (int, по умолчанию 0 → предзаказ)
* Скидка (%) — необязательно

НЕ добавлять создание товаров:

* клиенту
* цеху

4. ПЛАН (ТОЛЬКО экран "План" франчайзи):
   Добавить:

* Список планов (месяц/год + сумма)
* Кнопку "Добавить план"

Форма:

* месяц
* год
* сумма

НЕ добавлять:

* клиенту
* цеху

5. СТАТУСЫ (ТОЛЬКО франчайзи):

* УБРАТЬ возможность менять статус заказа
* Оставить только просмотр

==================================================
=== ЦЕХ (ТОЛЬКО ROLE = PRODUCTION) ===
======================================

1. СТАТУСЫ (ТОЛЬКО интерфейс цеха):
   Добавить выпадающий список:

* Заказ оформлен
* Пошив
* Готов
* Отправлен
* Получен

2. УПРАВЛЕНИЕ:

* Только ЦЕХ может менять статусы заказов

3. ОТОБРАЖЕНИЕ:

* Разделить заказы по статусам (списки или таблицы)

НЕ добавлять:

* этот функционал клиенту
* или франчайзи

==================================================
=== ЦЕХ + ФРАНЧАЙЗИ (ТОЛЬКО ЭТИ РОЛИ) ===
=========================================

АДАПТАЦИЯ:

* Убрать горизонтальный скролл
* Все элементы должны помещаться в ширину экрана
* Использовать вертикальные layout
* Интерфейс компактный

НЕ применять это отдельно к клиенту

==================================================
=== ГЛОБАЛЬНО (ВСЕ РОЛИ) ===
============================

1. ЯЗЫК:
   Добавить переключение:

* Казахский
* Русский
* Английский

Применяется ко ВСЕМ ролям

2. ФОТО:

* Убрать blur и эффекты ВЕЗДЕ

==================================================
=== ЗАПРЕТЫ ===
===============

* НЕ переносить функционал между ролями
* НЕ объединять интерфейсы
* НЕ добавлять новые экраны без необходимости
* НЕ менять существующую архитектуру

==================================================
=== ПОСЛЕ ИЗМЕНЕНИЙ ===
=======================

* Показать список изменённых файлов
* Кратко объяснить изменения по ролям
