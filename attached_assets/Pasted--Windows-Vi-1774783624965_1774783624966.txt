Нужно автоматически исправить проблему с пустым экраном после логина на локальном запуске Windows/Vite и подготовить проект к передаче на проверку в GitHub вместе с README.

Контекст:
- Это monorepo на pnpm.
- frontend лежит в artifacts/avishu
- backend лежит в artifacts/api-server
- frontend запускается через Vite
- роутинг сейчас на wouter
- после логина приложение уходит на /production
- маршрут /production существует
- vite.config.ts уже такой, что base = process.env.BASE_PATH ?? "/"
- в App.tsx используется:
  <WouterRouter base={import.meta.env.BASE_URL.replace(/\/$/, "")}>
- локально открывается http://localhost:5173/production, но экран пустой
- нужно НЕ объяснение, а именно внести правки в код

Что нужно сделать:
1. Найти и исправить причину пустого экрана на /production после логина.
2. Проверить страницу:
   - artifacts/avishu/src/pages/production/dashboard.tsx
   а также связанные с ней компоненты, хуки, запросы и редиректы.
3. Проверить, нет ли ошибок из-за:
   - неправильного base/router base
   - падения рендера внутри ProductionDashboard
   - обращения к undefined/null данным
   - бесконечного редиректа
   - ошибок useGetMe() или других запросов
   - неправильной работы авторизации после логина
4. Сделать так, чтобы локально на Windows приложение корректно работало через:
   - frontend: http://localhost:5173
   - логин: /login
   - после успешного логина production-user попадал на рабочий /production без черного/пустого экрана
5. Если проблема в router base или BASE_URL — исправить аккуратно, без поломки остальных ролей:
   - client
   - franchise
   - production
6. Проверить, чтобы маршруты /login, /register, /client, /franchise, /production работали локально без Replit-зависимостей.
7. Удалить или изолировать всё, что привязано только к Replit, если это мешает локальному запуску.
8. Сохранить проект чистым:
   - без replit-only костылей
   - без лишних артефактов
   - без временных заглушек
   - без сломанного роутинга
9. Подготовить README.md в корне проекта для передачи на проверку в GitHub.

README должен содержать:
- что это за проект
- структуру проекта
- требования: Node, pnpm
- как установить зависимости
- как запустить frontend
- как запустить backend
- какие переменные окружения нужны
- как собрать production build
- типовые проблемы на Windows и их решение
- короткий раздел “Clean GitHub handoff”

Важно:
- Внеси реальные изменения в код, а не просто дай рекомендации.
- После изменений покажи:
  1. список изменённых файлов
  2. кратко что именно исправлено в каждом
  3. финальные команды запуска
  4. полный текст README.md
- Не ломай текущую архитектуру.
- Не переводи проект на другой роутер, если можно исправить текущий.
- Не добавляй ничего специфичного для Replit в финальную версию.